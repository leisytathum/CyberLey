export default function DataTable({rows}){if(!rows?.length)return <div className="empty">No hay registros para mostrar.</div>; const cols=Object.keys(rows[0]).slice(0,8);return <div className="tableWrap"><table><thead><tr>{cols.map(c=><th key={c}>{c.replaceAll('_',' ')}</th>)}</tr></thead><tbody>{rows.map((r,i)=><tr key={r.id||i}>{cols.map(c=><td key={c}>{typeof r[c]==='object'?JSON.stringify(r[c]):String(r[c]??'')}</td>)}</tr>)}</tbody></table></div>}
