import { useEffect,useState } from 'react';import api from '../../services/api';
export default function DashboardPage(){const [s,setS]=useState(null),[error,setError]=useState('');useEffect(()=>{api.get('/dashboard/summary').then(r=>setS(r.data)).catch(e=>setError(e.message))},[]);return <><div className="pageTitle"><div><span className="eyebrow">Panel administrativo</span><h1>Inicio</h1><p>Vista general del proyecto CyberLey.</p></div></div>{error&&<div className="warningBox">{error}</div>}<div className="statsGrid">{[['Participantes',s?.participantes],['Encuestas',s?.encuestas],['Reportes',s?.reportes],['Riesgo alto',s?.riesgo_alto]].map(([a,b])=><article className="statCard" key={a}><small>{a}</small><strong>{b??'—'}</strong></article>)}</div><section className="panel"><h2>Estado del sistema</h2><p>React, FastAPI y Supabase están separados para mejorar rendimiento, mantenimiento y seguridad.</p></section></>}
