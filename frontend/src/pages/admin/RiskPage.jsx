import {useEffect,useState} from 'react';import api from '../../services/api';import DataTable from '../../components/common/DataTable';
export default function Page(){const [rows,setRows]=useState([]),[error,setError]=useState('');useEffect(()=>{api.get('/riesgo').then(r=>setRows(r.data.items||r.data||[])).catch(e=>setError(e.message))},[]);return <><div className="pageTitle"><div><span className="eyebrow">CyberLey</span><h1>Riesgo</h1><p>Análisis de puntajes y clasificaciones de riesgo.</p></div></div>{error&&<div className="warningBox">{error}</div>}<section className="panel"><DataTable rows={rows}/></section></>}
