import {useEffect,useState} from 'react';import api from '../../services/api';import DataTable from '../../components/common/DataTable';export default function AdministrationPage(){const [rows,setRows]=useState([]),[e,setE]=useState('');useEffect(()=>{api.get('/administracion/perfiles').then(r=>setRows(r.data.items)).catch(x=>setE(x.message))},[]);return <><div className="pageTitle"><div><span className="eyebrow">Configuración</span><h1>Administración</h1><p>Perfiles y roles registrados en CyberLey.</p></div></div>{e&&<div className="warningBox">{e}</div>}<section className="panel"><DataTable rows={rows}/></section></>}
