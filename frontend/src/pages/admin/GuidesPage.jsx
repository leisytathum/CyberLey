import { useEffect, useState } from "react";import api from "../../services/api";import { PageHeading, State } from "../../components/common/ModuleUI";
export default function GuidesPage(){const [rows,setRows]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState(""),[selected,setSelected]=useState(null);useEffect(()=>{api.get("/guias").then(r=>setRows(r.data.items||[])).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);return <><PageHeading eyebrow="Contenido" title="Guías de ciberseguridad">Consulta el contenido educativo disponible en Supabase.</PageHeading><State loading={loading} error={error} empty={!loading&&!rows.length} emptyText="No hay guías registradas todavía."/><div className="moduleGrid">{rows.map(x=><article className="moduleCard" key={x.id_guia}><span className="eyebrow">{x.categoria}</span><h3>{x.titulo}</h3><p>{x.descripcion||"Sin descripción."}</p><button onClick={()=>setSelected(x)}>Leer guía</button></article>)}</div>{selected&&<div className="moduleModal" onClick={()=>setSelected(null)}><article className="moduleModalContent" onClick={e=>e.stopPropagation()}><span className="eyebrow">{selected.nivel_recomendado}</span><h2>{selected.titulo}</h2><div className="guideContent">{selected.contenido||selected.descripcion}</div><button onClick={()=>setSelected(null)}>Cerrar</button></article></div>}</>}
