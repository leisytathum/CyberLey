import {useEffect,useState} from 'react';import api from '../../services/api';import DataTable from '../../components/common/DataTable';
export default function Page(){const [rows,setRows]=useState([]),[error,setError]=useState('');useEffect(()=>{api.get('/reportes').then(r=>setRows(r.data.items||r.data||[])).catch(e=>setError(e.message))},[]);return <><div className="pageTitle"><div><span className="eyebrow">CyberLey</span><h1>Reportes</h1><p>Historial y generación de reportes.</p></div></div>{error&&<div className="warningBox">{error}</div>}<section className="panel"><DataTable rows={rows}/></section></>}
