import {useState} from 'react';import api from '../../services/api';export default function CleaningPage(){const [r,setR]=useState(null);return <><div className="pageTitle"><div><span className="eyebrow">Calidad de datos</span><h1>Limpieza</h1><p>Diagnóstico seguro de registros duplicados o incompletos.</p></div></div><section className="panel"><button className="primary inline" onClick={()=>api.get('/limpieza/diagnostico').then(x=>setR(x.data))}>Ejecutar diagnóstico</button>{r&&<pre>{JSON.stringify(r,null,2)}</pre>}</section></>}
