import {useState} from "react";import api from "../../services/api";import {downloadBase64,Metrics,PageHeading,State} from "../../components/common/ModuleUI";
export default function BackupsPage(){const [file,setFile]=useState(null),[preview,setPreview]=useState(null),[message,setMessage]=useState(""),[error,setError]=useState("");async function exportBackup(){setError("");try{const data=(await api.get("/respaldos/exportar")).data;downloadBase64(data.contenido_base64,data.archivo,"application/gzip");setMessage("Respaldo generado correctamente.")}catch(e){setError(e.message)}}async function upload(path){if(!file){setError("Selecciona un archivo .gz.");return}const fd=new FormData();fd.append("archivo",file);try{const data=(await api.post(path,fd)).data;if(path.includes("previsualizar"))setPreview(data);else{setMessage("Respaldo restaurado correctamente.");setPreview(null)}}catch(e){setError(e.message)}}return <><PageHeading eyebrow="Seguridad" title="Respaldos">Genera y restaura copias comprimidas sin incluir contraseñas de autenticación.</PageHeading><State error={error}/>{message&&<div className="moduleState">{message}</div>}<div className="moduleGrid"><section className="moduleCard"><h2>Generar respaldo</h2><p>Incluye perfiles, participantes, evaluaciones y reportes.</p><button className="moduleButton" onClick={exportBackup}>Descargar respaldo .json.gz</button></section><section className="moduleCard"><h2>Restaurar respaldo</h2><input className="moduleInput" type="file" accept=".gz" onChange={e=>{setFile(e.target.files[0]||null);setPreview(null)}}/><button onClick={()=>upload("/respaldos/previsualizar")}>Validar archivo</button>{preview&&<><Metrics items={Object.entries(preview.conteos).map(([label,value])=>({label,value}))}/><button className="moduleButton danger" onClick={()=>window.confirm("¿Confirmas la restauración mediante upsert?")&&upload("/respaldos/restaurar")}>Restaurar datos</button></>}</section></div></>}
