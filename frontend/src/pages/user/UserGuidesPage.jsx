import { useEffect, useState } from "react";import api from "../../services/api";import { PageHeading, State } from "../../components/common/ModuleUI";
export default function UserGuidesPage(){const [rows,setRows]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState(""),[selected,setSelected]=useState(null);useEffect(()=>{api.cachedGet("/guias").then(r=>setRows(r.data.items||[])).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);return <><PageHeading eyebrow="Aprendizaje" title="Guías">Recursos reales para fortalecer tus hábitos digitales.</PageHeading><State loading={loading} error={error} empty={!loading&&!rows.length} emptyText="No hay guías disponibles todavía."/><div className="moduleGrid">{rows.map(x=><article className="moduleCard" key={x.id_guia}><span className="eyebrow">{x.categoria} · {x.nivel_recomendado}</span><h3>{x.titulo}</h3><p>{x.descripcion}</p><button onClick={()=>setSelected(x)}>Leer</button></article>)}</div>{selected&&<div className="moduleModal" onClick={()=>setSelected(null)}><article className="moduleModalContent" onClick={e=>e.stopPropagation()}><h2>{selected.titulo}</h2><div className="guideContent">{selected.contenido||selected.descripcion}</div><button onClick={()=>setSelected(null)}>Cerrar</button></article></div>}</>}
