import { Outlet, Link } from 'react-router-dom'; import { useAuth } from '../context/AuthContext'; export default function UserLayout(){const {profile,signOut}=useAuth();return <><header className="userHeader"><Link to="/usuario" className="logoText">CyberLey</Link><div><span>{profile?.nombre_completo||'Usuario'}</span><button className="linkButton" onClick={signOut}>Cerrar sesión</button></div></header><main className="userMain"><Outlet/></main></>}
